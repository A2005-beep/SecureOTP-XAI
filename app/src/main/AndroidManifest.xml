<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🔐 SMS Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <uses-permission android:name="android.permission.READ_SMS"/>

    <!-- 🌐 Network -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 🔔 Android 13+ Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- 🔋 Optional reliability improvement -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- 📳 Fraud alert vibration -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- 📡 Optional (tablet/no-SIM compatibility) -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- 📱 Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- 📊 Alerts Dashboard -->
        <activity android:name=".activities.AlertsActivity"/>

        <!-- 📱 App Risk Analyzer -->
        <activity android:name=".activities.AppRiskActivity"/>

        <!-- 🔐 OTP Protection -->
        <activity android:name=".activities.OTPProtectionActivity"/>

        <!-- 💣 SMS Analysis -->
        <activity android:name=".activities.SMSAnalysisActivity"/>

        <!-- 📡 SMS Receiver -->
        <receiver
            android:name=".receiver.SMSReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">

            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED"/>
                <action android:name="android.provider.Telephony.SMS_DELIVER"/>
            </intent-filter>

        </receiver>
    </application>

</manifest>