<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/cyber_bg_dark">

    <!-- Header Banner -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/cyber_surface"
        android:paddingTop="32dp"
        android:paddingBottom="24dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:elevation="8dp">

        <FrameLayout
            android:id="@+id/headerIconBg"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_icon_circle"
            android:backgroundTint="#1AEF4444"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="💣"
                android:textSize="20sp"/>
        </FrameLayout>

        <TextView
            android:id="@+id/headerTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="SMS Threat Analysis"
            android:textColor="@color/text_primary"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginStart="16dp"
            app:layout_constraintStart_toEndOf="@id/headerIconBg"
            app:layout_constraintTop_toTopOf="@id/headerIconBg"
            app:layout_constraintBottom_toTopOf="@+id/tvSmsStatus"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintVertical_chainStyle="packed"/>

        <TextView
            android:id="@+id/tvSmsStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Initializing NLP threat scanner..."
            android:textColor="@color/cyber_red"
            android:textSize="14sp"
            android:textStyle="bold"
            android:layout_marginStart="16dp"
            app:layout_constraintStart_toEndOf="@id/headerIconBg"
            app:layout_constraintTop_toBottomOf="@id/headerTitle"
            app:layout_constraintBottom_toBottomOf="@id/headerIconBg"
            app:layout_constraintEnd_toEndOf="parent"/>

        <ProgressBar
            android:id="@+id/progressSms"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="0dp"
            android:layout_height="4dp"
            android:layout_marginTop="20dp"
            android:indeterminate="true"
            android:progressTint="@color/cyber_red"
            android:indeterminateTint="@color/cyber_red"
            android:visibility="visible"
            app:layout_constraintTop_toBottomOf="@id/headerIconBg"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Summary strip -->
    <TextView
        android:id="@+id/tvSmsSummary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#1AEF4444"
        android:padding="12dp"
        android:textColor="@color/cyber_red"
        android:textSize="13sp"
        android:textStyle="bold"
        android:gravity="center"
        android:visibility="gone"
        android:text=""/>

    <!-- Results list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerSmsResults"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp"
        android:clipToPadding="false"/>

</LinearLayout>
